"""Full public dataset adapter scaffolds.

These adapters do not download large public datasets. They map user-provided
local public dataset files into the project common case schema so the repo stays
small and GitHub-runnable.
"""
